require "spec_helper"

describe Hell::Triangle do
  it "has a version number" do
    expect(Hell::Triangle::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
